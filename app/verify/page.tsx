'use client';import{useState}from'react';import{Card}from'@/components/ui/card';import{Input}from'@/components/ui/input';import{Button}from'@/components/ui/button';
export default function Verify(){const[applicationId,setA]=useState('');const[phoneNumber,setP]=useState('');const[result,setR]=useState<any>();async function go(){const r=await fetch('/api/verify',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({applicationId,phoneNumber})});setR(await r.json())}return <main className="mx-auto max-w-lg p-4 py-16"><Card><h1 className="text-2xl font-bold">Verify Application</h1><Input className="mt-4" placeholder="RHFSP-2026-XXXXXXXX" value={applicationId} onChange={e=>setA(e.target.value)}/><Input className="mt-3" placeholder="Phone number" value={phoneNumber} onChange={e=>setP(e.target.value)}/><Button onClick={go} className="mt-4 w-full">Verify</Button>{result&&<pre className="mt-4 overflow-auto rounded bg-slate-100 p-3 text-sm">{JSON.stringify(result,null,2)}</pre>}</Card></main>}
