import { NextResponse } from 'next/server'; import { adminSupabase } from '@/lib/supabase'; import { secureCode, sha256 } from '@/lib/crypto';
export async function POST(){const db=adminSupabase();await db.from('registration_lock_codes').update({is_active:false}).eq('is_active',true);const code=secureCode(14);const {data,error}=await db.from('registration_lock_codes').insert({code_hash:sha256(code),expires_at:new Date(Date.now()+3*60*60*1000).toISOString(),is_active:true}).select('id,expires_at').single();if(error) return NextResponse.json({error:error.message},{status:500});return NextResponse.json({code, ...data});}
