'use client';import {useState} from 'react';import {Card} from '@/components/ui/card';import {Input} from '@/components/ui/input';import {Button} from '@/components/ui/button';
export default function Access(){const[code,setCode]=useState('');const[msg,setMsg]=useState('');async function submit(){const r=await fetch('/api/registration-lock/validate',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code})});const j=await r.json();if(j.valid){sessionStorage.setItem('rhfsp_access',j.sessionToken);location.href='/register/terms'}else setMsg(j.error||'Denied')}return <main className="mx-auto max-w-lg p-4 py-16"><Card><h1 className="text-2xl font-bold">Registration Access Code</h1><p className="my-4 text-sm text-slate-600">Enter the current access code issued by your State Admin.</p><Input value={code} onChange={e=>setCode(e.target.value.toUpperCase())} placeholder="ACCESS CODE"/><Button onClick={submit} className="mt-4 w-full">Validate code</Button>{msg&&<p className="mt-3 text-red-600">{msg}</p>}</Card></main>}
